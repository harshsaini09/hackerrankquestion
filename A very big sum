#include<stdio.h>
int main()
{
    long int i,sum=0,n;
    scanf("%ld",&n);
    long int a[n];
    for(i=0;i<n;i++)
    {
      scanf("%ld",&a[i]);
      sum=sum+a[i];  
    }
    printf("%ld",sum);
}
